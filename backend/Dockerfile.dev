# Granger Backend - Dockerfile for development (with hot reload)
FROM node:20-alpine
WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY tsconfig.json ./
COPY src ./src
COPY tests ./tests
COPY vitest.config.ts ./

EXPOSE 4000
CMD ["npx", "tsx", "watch", "src/index.ts"]
